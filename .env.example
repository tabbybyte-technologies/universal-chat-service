# Server
PORT=3000
API_KEY=change-me

# LLM (Docker Model Runner)
DMR_MODEL=ai/qwen3:4B-UD-Q4_K_XL
# MODEL_BASE_URL and MODEL_ID are injected automatically by Docker Compose via the
# models: block. Override them here only when running outside of Compose.
#MODEL_BASE_URL=http://model-runner.docker.internal/engines/v1
#MODEL_ID=ai/qwen3:4B-UD-Q4_K_XL

# Redis
REDIS_URL=redis://localhost:6379

# Memory
MEMORY_MAX_MESSAGES=10
